@model Traffic_ASP_MVC.Models.Coordinators

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Coordinators</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CoordinatorName" class="control-label"></label>
                <input asp-for="CoordinatorName" class="form-control" />
                <span asp-validation-for="CoordinatorName" class="text-danger"></span>
            </div>
            <div class="form-group mt-4">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-warning mt-1" asp-controller="Home" asp-action="Index">Home</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
